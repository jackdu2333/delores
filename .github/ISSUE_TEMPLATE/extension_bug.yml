name: Legacy Extension Bug
description: Report an issue in the parked Raycast extension pack
title: "[Extension]: "
labels: ["bug", "extension"]
body:
  - type: markdown
    attributes:
      value: <sub>Delores currently targets macOS 26+.</sub>
  - type: markdown
    attributes:
      value: |
        **Read this first.** Raycast Extensions are currently parked under
        `Packs/LegacyFeatures/` and are not part of the active Delores target. This template is for
        regressions in that retained pack, not for requesting that the feature be restored.

        The compatibility notes are in
        [the legacy extension documentation](https://github.com/jackdu2333/delores/blob/main/Packs/LegacyFeatures/docs/extensions.md).
        An extension that reaches for something only Raycast has cannot be made to work here, and
        the issue is closed as `wontfix`:

        - Raycast AI — `AI.ask`, AI commands, `tools/` AI extensions
        - the Raycast browser extension (`BrowserExtension`)
        - Raycast's `WindowManagement` service
        - Raycast's OAuth proxy, `oauth.raycast.com`
        - Raycast Pro, cloud sync, accounts or teams
        - an OS permission or entitlement the legacy pack doesn't hold
        - Raycast-internal APIs, private modules and unpublished behaviour

        The full list of gaps is in the legacy extension documentation. A regression in the retained
        runtime is worth reporting. "It works in
        Raycast" on its own is not.
  - type: checkboxes
    id: existing-search
    attributes:
      label: Existing issues
      options:
        - label: I searched open and closed issues and didn't find this already reported
          required: false
  - type: input
    id: extension-url
    attributes:
      label: Extension URL
      description: Its page in the Raycast store, or the repository it comes from.
      placeholder: https://www.raycast.com/author/extension
    validations:
      required: true
  - type: textarea
    id: what-happened
    attributes:
      label: What happened?
      placeholder: |
        What you did, what you expected, what you got instead.
        A short recording beats a paragraph.
    validations:
      required: true
  - type: textarea
    id: repro-steps
    attributes:
      label: Steps to reproduce
      placeholder: |
        1.
        2.
        3.
    validations:
      required: true
  - type: textarea
    id: screenshots
    attributes:
      label: Screenshots or recording
      description: >
        Required. Drag and drop a screenshot or a screen recording of the extension failing — an
        extension bug with nothing to look at is closed.
    validations:
      required: true
  - type: input
    id: version
    attributes:
      label: Delores version + channel
      placeholder: 0.1.0 stable
    validations:
      required: true
  - type: input
    id: macos
    attributes:
      label: macOS version
      placeholder: "26.0"
    validations:
      required: true
  - type: checkboxes
    id: extension-ack
    attributes:
      label: Extension limits
      options:
        - label: I understand this is closed if the extension depends on Raycast-only capabilities
          required: true
  - type: checkboxes
    id: willing
    attributes:
      label: Contribution
      description: >
        **Ticking this is not permission to start.** Wait until a maintainer labels this issue
        `approved` — that label is the only green light. A pull request whose linked issue isn't
        `approved` is closed automatically, unreviewed. Docs-only changes are the exception. Read
        [CONTRIBUTING.md](https://github.com/jackdu2333/delores/blob/main/CONTRIBUTING.md)
        first — it is required reading, and it is what your PR is held to.
      options:
        - label: I'm willing to implement this myself **once this issue is labelled `approved`**
          required: false
